# Expense Tracker (Python)

A simple command-line expense tracker built with Python.  
It allows you to add, view, and summarize expenses stored in a CSV file.

## Features
- Add expenses with category and date
- View all expenses
- Generate summary reports (category-wise totals)

## How to Run
```bash
python main.py
